[project]
name = "laboratorio03si"
version = "0.1.0"
description = ""
authors = [
    {name = "EricRicaldi",email = "eericaldir@ucvvirtual.edu.pe"}
]
requires-python = ">=3.14"
dependencies = [
    "kedro (>=1.6.0,<2.0.0)",
    "pandas (>=3.0.6,<4.0.0)",
    "numpy (>=2.5.3,<3.0.0)",
    "matplotlib (>=3.11.2,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)"
]
